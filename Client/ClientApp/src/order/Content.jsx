import React, {useState} from 'react';
import cl from "./Components.module.css";
import {Box} from '@mui/material';

const Content = () => {

    return (
        <div className={cl.contentBox}> Плати </div>
    );
};

export default Content;